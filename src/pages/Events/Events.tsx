import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Events = () => {
  return (
    <SafeAreaView>
      <Text>Events</Text>
    </SafeAreaView>
  );
};

export default Events;
